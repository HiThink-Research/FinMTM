# Eval Agent Pipeline  
**多模型 · 多轮金融 Agent 推理与轨迹评分流水线**

本项目是一套 **可直接运行** 的金融 VLM-Agent 评测框架，支持对多个模型按顺序执行：

1. **Stage 1 — Inference（多轮 Agent 推理 + MCP 工具调用）**  
   - 模型根据 *问题 + 图像* 输出 `ActionTrace`
   - 系统通过 MCP Server 执行工具
   - 工具结果被注入下一轮 Prompt
   - 直到模型输出 `<FINISHED>` 或达到最大轮次

2. **Stage 2 — Judge（基于轨迹的自动化评分）**  
   - Judge（默认 Gemini）读取推理轨迹
   - 按 **Answer / Tool / Reasoning** 三个维度评分
   - 输出结构化 JSON 分数与评分依据

---

## 1. 项目结构

```
eval_agent_pipeline/
  __init__.py
  config.py          # 全局配置（MCP 地址、MAX_ITER、默认路径）
  logging_utils.py   # 日志配置
  utils.py           # JSONL 读写、safe_parse_json 等工具函数
  prompts.py         # Agent Prompt & Judge Prompt
  mcp_client.py      # MCP 连接与工具调用（含重试）
  agent.py           # MultiRoundAgent：多轮规划与执行
  inference.py       # Stage 1：批量推理
  judge.py           # Stage 2：批量评分
  models.py          # 定义要评测的模型列表
  main.py            # CLI 主入口
```

---

## 2. 环境要求

- Python ≥ 3.8（推荐 3.10+）
- MCP Server 已启动（默认地址）  
  ```
  http://localhost:8081/sse
  ```
- 以下 Python 包可被 import：
  - `aiofiles`
  - `tqdm`
  - `httpx`
  - `mcp`
- 你自己的模型封装：
  - `api.py`  
    - `GPT4OClient`
    - `GEMINIClient`
    - `Grok`
  - `qwen3vl.py`  
    - `Qwen3VLClient`

安装基础依赖示例：

```bash
pip install aiofiles tqdm httpx
```

> `mcp / api2 / qwen3vl` 通常是内部或本地依赖，请确保已加入 `PYTHONPATH`

---

## 3. 全局配置

配置文件位置：

```
eval_agent_pipeline/config.py
```

关键参数：

- `MCP_SERVER_URL`：MCP SSE 地址
- `MAX_ITER`：每个样本最大多轮数（默认 8）
- `DEFAULT_INPUT_FILE`：默认输入 JSONL
- `DEFAULT_RESULT_FILE`：默认输出目录

**通常不需要修改，CLI 参数可全部覆盖。**

---

## 4. 输入数据格式（JSONL）

输入文件必须是 **JSONL**（一行一个样本）。

### 推荐格式（带 turn）

```json
{
  "image_path": "/abs/path/to/image.png",
  "turn": {
    "question": "图中公司是哪家？并给出结论。",
    "VisualObservation": ["..."],
    "Thought": "...",
    "ActionTrace": [
      {"tool": "Search", "query": "xxx"}
    ],
    "gold_answer": {
      "final_conclusion": "..."
    }
  }
}
```

### 简化格式（扁平）

```json
{
  "image_path": "/abs/path/to/image.png",
  "question": "图中公司是哪家？并给出结论。",
  "gold_answer": "..."
}
```

说明：

- `image_path` 可以是字符串或 list（取第一个）
- 系统会优先读取 `turn.question`
- `gold_answer` 用于 Judge 阶段评分

---

## 5. 输出文件说明

每个模型在 `--out_root` 下生成一个子目录：

```
out_root/
  gpt5/
    trace.jsonl   # Stage 1 推理轨迹
    score.jsonl   # Stage 2 评分结果
  gemini/
    trace.jsonl
    score.jsonl
```

如果使用 `--with_ts`：

```
out_root/gpt5_20260112_103000/
```

---

## 6. 快速开始（直接可用）

### 6.1 推理 + 评分（推荐）

```bash
python -m eval_agent_pipeline.main \
  --mode all \
  --input /path/to/agent.jsonl \
  --out_root /path/to/out \
  --models gpt5,gemini \
  --with_ts
```

参数说明：

| 参数 | 说明 |
|----|----|
| `--mode` | `all / inference / eval` |
| `--input` | 输入 JSONL |
| `--out_root` | 输出根目录 |
| `--models` | 指定模型 key（逗号分隔） |
| `--with_ts` | 输出目录添加时间戳 |

---

### 6.2 只跑推理（Stage 1）

```bash
python -m eval_agent_pipeline.main \
  --mode inference \
  --models gpt5 \
  --input /path/to/agent.jsonl
```

---

### 6.3 只跑评分（Stage 2）

> 需要已有 `trace.jsonl`

```bash
python -m eval_agent_pipeline.main \
  --mode eval \
  --models gpt5 \
  --input /path/to/agent.jsonl
```

---

## 7. 模型配置

模型列表定义在：

```
eval_agent_pipeline/models.py
```

示例：

```python
def build_models():
    return [
        {"key": "gpt5", "obj": GPT4OClient(model="gpt-5")},
        {"key": "gemini", "obj": GEMINIClient()},
        {"key": "grok", "obj": Grok()},
    ]
```

### 新增模型要求

模型对象必须实现接口：

```python
await client.image2text(
    instruction=...,
    image=...,
    temperature=0.0,
    pbar=None,
    output_file=None
)
```

---

## 8. Agent 输出格式（关键）

Agent **必须输出一个 JSON 对象**：

```json
{
  "Thought": "推理过程",
  "VisualObservation": ["图像中的关键信息"],
  "ActionTrace": [
    {"tool": "FinQuery", "query": "查询xxx"}
  ]
}
```

任务完成时，模型应输出：

```
<FINISHED>
```

---

## 9. Judge 评分规则（简述）

总分 **100 分**

### Answer（0 / 50）
- 核心实体 + 数值完全一致 → 50
- 否则 → 0

### Tool（0 ~ 25）
- 基于 Tool F1：
  ```
  tool_score = round(F1 * 25)
  ```
- 同时输出：
  - Recall
  - Precision
  - F1
  - EMR（严格序列匹配）

### Reasoning（0 ~ 25）
- Judge 先给 `raw_reason ∈ [0,1]`
- 映射为 `round(raw_reason * 25)`

---

## 10. 常见问题

### Q1：MCP 连接失败？
- 确认 MCP Server 已启动
- 检查 `config.py` 中的 `MCP_SERVER_URL`

### Q2：模型输出不是 JSON？
- 使用 `safe_parse_json()` 自动抽取
- 支持 ```json 代码块 和文本中 JSON

### Q3：多个模型会并行吗？
- **不会**，默认串行执行（最稳，不抢资源）

---

## 11. 适用场景

- 金融多轮 Agent 评测
- Tool-use / MCP / Agent 规划能力对比
- 多模型统一 benchmark
- 学术评测 / 内部模型对齐

---

## 12. License

内部研究或工程项目使用，若需开源请补充 License。
